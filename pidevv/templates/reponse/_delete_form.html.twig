<form method="post" action="{{ path('app_reponse_delete', {'id': reponse.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer votre réponse?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.id) }}">
    <button class="btn"><img src="{{ asset('assets/img/delete.png') }}" height="30" width="30"></button>
</form>
